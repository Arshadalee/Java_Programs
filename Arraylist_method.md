## 1. arrayList.size():
Returns the number of elements in the ArrayList.

## 2. arrayList.add(E element):
Appends the specified element to the end of the ArrayList.

## 3. arrayList.add(int index, E element):
Inserts the specified element at the specified position in the ArrayList.

## 4. arrayList.remove(int index):
Removes the element at the specified position in the ArrayList.

## 5. arrayList.remove(Object o):
Removes the first occurrence of the specified element from the ArrayList, if it is present.

## 6. arrayList.get(int index):
Returns the element at the specified position in the ArrayList.

## 7. arrayList.set(int index, E element):
Replaces the element at the specified position in the ArrayList with the specified element.

## 8. arrayList.contains(Object o):
Returns true if the ArrayList contains the specified element.

9. arrayList.isEmpty():
Returns true if the ArrayList contains no elements.

10. arrayList.clear():
Removes all elements from the ArrayList.

## 11. arrayList.indexOf(Object o):
Returns the index of the first occurrence of the specified element, or -1 if the element is not found.

## 12. arrayList.lastIndexOf(Object o):
Returns the index of the last occurrence of the specified element, or -1 if the element is not found.

## 13. arrayList.subList(int fromIndex, int toIndex):
Returns a view of the portion of the ArrayList between the specified fromIndex (inclusive) and toIndex (exclusive).

## 14. arrayList.addAll(Collection<? extends E> c):
Appends all of the elements in the specified collection to the ArrayList.

## 15. arrayList.removeAll(Collection<?> c):
Removes from the ArrayList all of its elements that are contained in the specified collection.

## 16. arrayList.sort(Comparator<? super E> c):
Sorts the ArrayList according to the order specified by the provided comparato
